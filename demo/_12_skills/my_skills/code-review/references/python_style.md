# Python代码风格参考

## PEP 8 核心规则摘要

- 缩进使用4个空格
- 行宽不超过79字符
- import 分三组：标准库、第三方、本地
- 类之间空两行，方法之间空一行
- 字符串优先用双引号

## 常见反模式

- `except:` 不指定异常类型（吞掉所有异常）
- 可变默认参数 `def foo(items=[])`
- 使用 `type()` 代替 `isinstance()` 做类型检查
- 在循环中拼接字符串（应用 join）
